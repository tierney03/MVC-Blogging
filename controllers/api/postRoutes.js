const router = require("express").Router();
const { Post } = require("../../models");


router.post (create)

router.get (pulling 1 post)

router.delete (delete a post)

router.put (update a post)

modules.export = router;